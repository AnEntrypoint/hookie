[
  {
    "id": "refactor-layout-shells",
    "subject": "Replace adminAppStyles.js and builderStyles.js with Tailwind classes",
    "status": "pending",
    "description": "AdminApp.js and Builder.js use *Styles.js for layout. Convert all to Tailwind className strings, delete the style files. Builder passes styles.right/left/placeholder to sub-components — those must be updated too.",
    "blocking": ["delete-responsive-styles"],
    "blockedBy": [],
    "effort": "small",
    "category": "refactor",
    "acceptance": [
      "adminAppStyles.js deleted",
      "builderStyles.js deleted",
      "No style= props for layout in AdminApp.js or Builder.js or BuilderPropsPanel.js",
      "3-column builder layout visually intact"
    ],
    "edge_cases": [
      "Builder passes styles object as prop to BuilderPropsPanel — must convert that component too"
    ]
  },
  {
    "id": "refactor-page-manager",
    "subject": "Replace pageManagerStyles.js and pageCardStyles.js with Ripple UI + Tailwind",
    "status": "pending",
    "description": "PageManager: grid, modal, form, error, empty state. PageCard: card layout, confirm dialog, buttons. Convert to Ripple UI classes (btn, card, modal-box, alert, input input-bordered, form-group). Delete both *Styles.js files.",
    "blocking": ["delete-responsive-styles"],
    "blockedBy": [],
    "effort": "medium",
    "category": "refactor",
    "acceptance": [
      "pageManagerStyles.js deleted",
      "pageCardStyles.js deleted or does not exist",
      "PageManager.js and PageCard.js have zero style= props",
      "Page manager modal, grid, and form render correctly"
    ],
    "edge_cases": [
      "CSS spinner animation — replace with Tailwind animate-spin",
      "Linear gradient on header — may need to keep as inline style"
    ]
  },
  {
    "id": "refactor-builder-canvas",
    "subject": "Replace builderCanvasStyles.js with Tailwind classes in BuilderCanvas.js",
    "status": "pending",
    "description": "BuilderCanvas toolbar, select, empty state, quick-add buttons all use JS style objects. Convert to Tailwind. Delete builderCanvasStyles.js.",
    "blocking": ["delete-responsive-styles"],
    "blockedBy": [],
    "effort": "small",
    "category": "refactor",
    "acceptance": [
      "builderCanvasStyles.js deleted",
      "BuilderCanvas.js uses only className= strings (except dynamic inline bg color for page background)",
      "Toolbar, empty state, quick-add visually match"
    ],
    "edge_cases": [
      "Canvas bg color is dynamic (layout.colors.background) — must stay as inline style"
    ]
  },
  {
    "id": "refactor-settings",
    "subject": "Confirm and delete settingsStyles.js (Settings.js already uses Ripple UI)",
    "status": "pending",
    "description": "Settings.js was already refactored to Ripple UI classes. Verify settingsStyles.js is no longer imported anywhere, then delete it.",
    "blocking": ["delete-responsive-styles"],
    "blockedBy": [],
    "effort": "small",
    "category": "refactor",
    "acceptance": [
      "settingsStyles.js deleted",
      "No imports of settingsStyles anywhere",
      "Settings page renders correctly"
    ],
    "edge_cases": [
      "Other files may still import settingsStyles — grep required before delete"
    ]
  },
  {
    "id": "refactor-modals",
    "subject": "Replace keyboardShortcutsStyles.js and componentLibraryStyles.js with Tailwind + Ripple",
    "status": "pending",
    "description": "KeyboardShortcutsModal: overlay + grouped kbd pairs. ComponentLibrary/List/Detail: card grid, badges, detail panel. Convert modals to fixed inset-0 backdrop + card modal-box. Use Ripple kbd class for key badges.",
    "blocking": ["delete-responsive-styles"],
    "blockedBy": [],
    "effort": "medium",
    "category": "refactor",
    "acceptance": [
      "keyboardShortcutsStyles.js deleted",
      "componentLibraryStyles.js deleted",
      "KeyboardShortcutsModal renders with Ripple kbd styling",
      "ComponentLibrary grid and detail render correctly"
    ],
    "edge_cases": [
      "Ripple kbd class availability must be verified before use"
    ]
  },
  {
    "id": "refactor-palette",
    "subject": "Replace componentPaletteStyles.js with Tailwind in ComponentPalette + PaletteComponentCard",
    "status": "pending",
    "description": "Palette grid, search, category headers, draggable cards, mobile bottom sheet. Convert static styles to Tailwind classes. Mobile bottom sheet (position:fixed + slideUp animation) must stay as CSS or inline. DnD opacity stays inline (dynamic).",
    "blocking": ["delete-responsive-styles"],
    "blockedBy": [],
    "effort": "medium",
    "category": "refactor",
    "acceptance": [
      "componentPaletteStyles.js deleted",
      "ComponentPalette.js and PaletteComponentCard.js use className= for all static styles",
      "Palette drag-and-drop works, category collapse works, mobile sheet works"
    ],
    "edge_cases": [
      "Mobile bottom sheet animation cannot be expressed in Tailwind — keep in admin.css"
    ]
  },
  {
    "id": "delete-responsive-styles",
    "subject": "Delete responsiveStyles.js after all consumers migrated",
    "status": "pending",
    "description": "responsiveStyles.js exports minTouchSize and breakpoints. After all *Styles.js files are deleted and consumers converted, verify no remaining imports and delete the file.",
    "blocking": [],
    "blockedBy": ["refactor-layout-shells", "refactor-page-manager", "refactor-builder-canvas", "refactor-settings", "refactor-modals", "refactor-palette"],
    "effort": "small",
    "category": "refactor",
    "acceptance": [
      "responsiveStyles.js deleted",
      "No remaining imports of responsiveStyles",
      "Build succeeds (exec:bash npm run build exits 0)"
    ],
    "edge_cases": [
      "adminHeaderStyles.js still imports minTouchSize — if header styles not in scope, keep that import"
    ]
  }
]
